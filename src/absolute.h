#ifndef ABSOLUTE_H

#define ABSOLUTE_H

/**
 * Calculates the absolute of a given float.
 * @param value The angle to calculate the absolute of.  Behavior is undefined
 *              if NaN.
 * @return The absolute of the given float.
 */
float absolute(const float value);

#endif
