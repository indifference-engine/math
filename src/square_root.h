#ifndef SQUARE_ROOT_H

#define SQUARE_ROOT_H

/**
 * Calculates the approximate square root of a float.
 * @param value The float of which to calculate an approximate square root.
 *              Behavior is undefined if NaN, infinity or negative.
 * @return The calculated approximate square root.
 */
float square_root(const float value);

#endif
